switch.pd est une abstraction utile dans le patch principal
Le fichier mapping.txt permets de savoir a quelle touche de manette est associée la touche clavier dans le logiciel de mapping (JoyToKey) et donc dans le Patch.

switch.pd gère la detection du mode boule de feu
